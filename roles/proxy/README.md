# Ansible Collection - redpanda.proxy

Configures the client with an insecure proxy and ssh proxying. This should not be used for production and is intended only for demoing and testing an environment with limited connectivity routed through a proxy. 

If you want a more secure and generally usable https proxy consider something like an nginx reverse proxy. 
